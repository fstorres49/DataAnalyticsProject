import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CodeChallengeND',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
